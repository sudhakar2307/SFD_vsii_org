public class Exception_Ex3 {
    string s;
    public void ex3mth(){
        try{
       system.debug(s.toUpperCase());
        }
        catch(exception e ){
            system.debug('there are no string value with the linked values ');
           system.debug(e.getMessage());
        }
        
    }

}