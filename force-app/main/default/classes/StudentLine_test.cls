@isTest
public class StudentLine_test {
    public static testmethod Void main(){
        StudentLine s1=new StudentLine();
        system.debug('obj------>' +s1.obj);
        system.debug('the name ---> '+s1.obj.name);
        system.debug('the age ----> '+s1.obj.age);
        
    }

}